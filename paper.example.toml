host = "127.0.0.1"
port = 3000
data_dir = "./data/"
# database_url = "sqlite:./data/db/data.db?mode=rwc"
# database_url = "postgres://user:password@localhost/paper"
# database_url = "mysql://user:password@localhost/paper"

# Hardcover GraphQL token — https://hardcover.app → account settings → API.
# Search works without it; full metadata details require it.
hardcover_api_key = "your-hardcover-api-token"
