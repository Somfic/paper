[package]
name = "paper"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "paper"
path = "src/main.rs"

[dependencies]
# Schema-driven RPC. `include_generated!` drives the whole scan → emit
# pipeline at macro-expansion time, so there's no build.rs and no ts-rs.
# The TS client is written to `frontend/src/lib/schema/index.ts` as a side
# effect of `cargo build`. The `runtime` feature ships the EventBus + the WS
# Session/Conn/Conns system.
draad = { git = "https://github.com/somfic/draad", tag = "v0.3.0", features = ["runtime"] }
async-trait = "0.1"

# Web
axum = { version = "0.8.9", features = ["ws", "macros", "multipart"] }
tower-http = { version = "0.6", features = ["fs"] }
reqwest = { version = "0.13.4", features = ["json", "stream"] }

# Database
sqlx = { version = "0.8.6", features = ["any", "sqlite", "postgres", "mysql", "runtime-tokio", "migrate"] }

# Async
tokio = { version = "1.52.3", features = ["full"] }
futures = "0.3"
tokio-util = { version = "0.7", features = ["io"] }

# Serialization
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
toml = "1.1.0"

# CLI
clap = { version = "4", features = ["derive", "env"] }

# Error handling
thiserror = "2.0.18"
anyhow = "1"

# Logging
tracing = "0.1.44"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
nu-ansi-term = "0.50"

# Misc
urlencoding = "2.1.3"
bytes = "1"
mime_guess = "2"
